# Custom-16-bit-System-Emulator
This repository implements the general system around a 16-bit CPU (Bus, RAM, Cache) as well as a custom assembler and higher level IR compiler, as well as a cascading transpilation pipeline
